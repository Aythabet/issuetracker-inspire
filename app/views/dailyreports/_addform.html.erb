<%= simple_form_for @dailyreport do |f| %>
  <%= f.association :owner, selected: current_user.id  %>
  <%= f.input :comment %>
  <%= f.association :issues, as: :check_boxes %>
  <%= f.button :submit %>
<% end %>