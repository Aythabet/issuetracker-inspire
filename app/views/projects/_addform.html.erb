<%= simple_form_for @project do |f| %>

  <%= f.input :jiraid, label: 'Enter the JIRA ID', error: 'The JIRA ID is mandatory, please specify one'%>
  <%= f.input :name, label: 'Enter the project name', error: 'The name is mandatory, please specify one'%>

  <%= f.button :submit %>

<% end %>